import { createVariants } from '../utils/create-variants'

export const iconVariants = createVariants({
  base: [
    'outline-blue-500 outline-offset-4 focus-visible:outline-blue-500',
    'size-4 rounded-full'
  ]
})
